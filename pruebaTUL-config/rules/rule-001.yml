apiVersion: v1
kind: rule
metadata:
  id: "rule-001"
  name: "Regla 0120"
  description: "Descripcion de la regla"
  domain: "DBB"
  owner: "equipo-x"
  version: "1.0.0"
  status: "active"
spec:
  effect: "permit"
  decisionReason: "Motivo de la decision"
  condition:
    operator: "equals"
    attribute: "channel"
    value: "web"
